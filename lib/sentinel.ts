import {Project} from "@/data/projects"; export type Evidence={title:string;detail:string;weight:number}; export function analyzeProject(p:Project){const e:Evidence[]=[];const util=p.utilized/p.budget*100;const variance=Math.max(0,(p.actual-p.planned)/p.planned*100);if(p.progress<40)e.push({title:"Low physical progress",detail:`Only ${p.progress}% complete`,weight:22});if(util-p.progress>25)e.push({title:"Fund vs physical mismatch",detail:`${Math.round(util)}% funds utilized vs ${p.progress}% physical progress`,weight:24});if(p.stale>=14)e.push({title:"Stale field update",detail:`No verified field update for ${p.stale} days`,weight:18});if(variance>20)e.push({title:"Schedule variance",detail:`${Math.round(variance)}% beyond planned duration`,weight:18});if(p.status==="Delayed"||p.status==="Critical")e.push({title:"Status escalation signal",detail:`Current status is ${p.status}`,weight:10});const score=Math.min(99,e.reduce((s,x)=>s+x.weight,0));const level=score>=80?"Critical":score>=60?"Delayed":score>=35?"At Risk":"On Track";return {score,level,project:p,evidence:e,recommendation:score>=80?"Initiate field verification, review utilization evidence and require a recovery plan.":score>=60?"Schedule an inspection and request an updated implementation plan.":score>=35?"Increase monitoring frequency and request the next field update.":"Continue routine monitoring; no immediate escalation is indicated."};}