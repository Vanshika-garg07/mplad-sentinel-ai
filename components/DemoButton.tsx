"use client"; import {useState} from "react"; import {projects} from "@/data/projects"; import {analyzeProject} from "@/lib/sentinel"; export default function DemoButton(){const[busy,setBusy]=useState(false);const[show,setShow]=useState(false);const[r,setR]=useState<any>(null);function run(){setBusy(true);setTimeout(()=>{const x=analyzeProject(projects.find(p=>p.id==="MP-2026-003")!);localStorage.setItem("mplad-sentinel-latest-analysis",JSON.stringify(x));setR(x);setShow(true);setBusy(false)},800)}return <><button className="primary" onClick={run} disabled={busy}>{busy?"Analyzing evidence…":"🚀 Run AI Demo"}</button>{show&&r&&<div className="modal-backdrop" onClick={()=>setShow(false)}><div className="modal" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setShow(false)}>×</button><span className="tag critical">CRITICAL</span><h2>Critical delivery risk detected</h2><p>{r.project.id} · {r.project.name}</p><div className="score"><strong>{r.score}</strong><span>/100 · {r.level}</span></div><div className="evidence">{r.evidence.map((e:any)=><div key={e.title}><b>⚠ {e.title}</b><small>{e.detail}</small></div>)}</div><div className="recommend"><b>Recommended authority action</b><p>{r.recommendation}</p></div></div></div>}</>}